,target,doc
0,1,Move PropertyPlaceholder and MessageSource auto-configuration from root package
1,1,There are currently only two auto-configurations (`PropertyPlaceholderAutoConfiguration` and `MessageSourceAutoConfiguration`) in the root `org.springframework.boot.autoconfigure` package. They'd be better off in the `context` sub-package.
